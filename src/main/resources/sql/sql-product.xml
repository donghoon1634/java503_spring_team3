<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bitc.fullstack503.java503_team3.mapper.ProductMapper">

    <!-- 상품 목록 조회 -->
    <select id="selectProductList" resultType="bitc.fullstack503.java503_team3.dto.ProductDTO">
        SELECT * FROM product ORDER BY productNum DESC
    </select>

    <!-- 상품 등록 -->
    <insert id="insertProduct">
        INSERT INTO product (productName, categoryNum, productPrice, productImg, localGuNum, localGuName, member_idx)
        VALUES (#{productName}, #{categoryNum}, #{productPrice}, #{productImg}, #{localGuNum}, #{localGuName}, #{member_idx})
    </insert>

    <!-- 상품 상세 조회 -->
    <select id="selectProductDetail" parameterType="int" resultType="bitc.fullstack503.java503_team3.dto.ProductDTO">
        SELECT * FROM product WHERE productNum = #{productNum}
    </select>

    <!-- 상품 삭제 -->
    <delete id="deleteProduct">
        DELETE FROM product WHERE productNum = #{productNum}
    </delete>

    <!-- 상품 수정 -->
    <update id="updateProduct">
        UPDATE product
        SET productName = #{productName}, categoryNum = #{categoryNum}, productPrice = #{productPrice},
        productImg = #{productImg}, localGuNum = #{localGuNum}, localGuName = #{localGuName}
        WHERE productNum = #{productNum}
    </update>

    <!-- 카테고리별 상품 목록 조회 -->
    <select id="selectProductListByCategory" parameterType="int" resultType="bitc.fullstack503.java503_team3.dto.ProductDTO">
        SELECT * FROM product
        WHERE categoryNum = #{categoryNum}
        ORDER BY productNum DESC
    </select>

    <!-- 가격대별 상품 목록 조회 -->
    <select id="selectProductListByPrice" parameterType="map" resultType="bitc.fullstack503.java503_team3.dto.ProductDTO">
        SELECT * FROM product
        WHERE productPrice BETWEEN #{minPrice} AND #{maxPrice}
        ORDER BY productNum DESC
    </select>

    <!-- 지역별 상품 목록 조회 -->
    <select id="selectProductListByLocalGu" parameterType="int" resultType="bitc.fullstack503.java503_team3.dto.ProductDTO">
        SELECT * FROM product WHERE localGuNum = #{localGuNum} ORDER BY productNum DESC
    </select>

</mapper>
