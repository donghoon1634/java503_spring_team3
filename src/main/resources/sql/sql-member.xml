<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bitc.fullstack503.java503_team3.mapper.MemberMapper">
  <select id="isMemberId" parameterType="java.lang.String" resultType="int">
    select count(*)
    from member
    where member_id = #{memberId}
  </select>
  <select id="isMemberNickname" parameterType="java.lang.String" resultType="int">
    select count(*)
    from member
    where member_nickname = #{memberNickname}
  </select>
  <insert id="signUp" parameterType="bitc.fullstack503.java503_team3.dto.MemberDTO">
    insert into member (member_id, member_pw, member_nickname, member_phone, member_addr, member_addr_detail)
    values (#{memberId}, #{memberPw}, #{memberNickname}, #{memberPhone}, #{memberAddr}, #{memberAddrDetail})
  </insert>
  <insert id="signUpInsert" parameterType="bitc.fullstack503.java503_team3.dto.MemberDTO">
    INSERT INTO member_profile (member_profile_href, member_profile_member_id)
    VALUES ('/member/basic.jpg', #{memberId});
  </insert>
  
  <select id="signIn" parameterType="bitc.fullstack503.java503_team3.dto.MemberDTO"
          resultType="int">
    select count(*)
    from member
    where member_id = #{memberId}
      and member_pw = #{memberPw}
  </select>
  
  <select id="memberInfo" resultType="bitc.fullstack503.java503_team3.dto.MemberDTO">
    select *
    from member
    where member_id = #{memberId}
  </select>
  
  <insert id="memberProfile" parameterType="bitc.fullstack503.java503_team3.dto.MemberProfileDTO">
    INSERT INTO member_profile (member_profile_href, member_profile_member_id)
    VALUES
    <foreach collection="list" item="item" separator=",">
      (#{item.memberProfileHref}, #{item.memberProfileMemberId})
    </foreach>
    ON DUPLICATE KEY UPDATE
    member_profile_href = VALUES(member_profile_href);
  </insert>
  
  
  <select id="memberProfileHref" parameterType="java.lang.String" resultType="java.lang.String">
    select member_profile_href
    from member_profile
    where member_profile_member_id = #{memberProfileMemberId};
  </select>
</mapper>